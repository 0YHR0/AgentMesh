"""Buildable A3 reference Agent process.

The package deliberately depends on the public ``agentmesh.runtime_sdk`` and
the Python standard library only.  It is a protocol fixture, not a business
agent or a sandbox for untrusted code.
"""

