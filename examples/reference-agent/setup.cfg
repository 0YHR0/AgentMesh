[metadata]
name = agentmesh-reference-agent
version = 0.1.0a1
description = Standalone AgentMesh Runtime SDK JSONL reference Agent

[options]
package_dir =
    = src
packages = find:
python_requires = >=3.10

[options.packages.find]
where = src
